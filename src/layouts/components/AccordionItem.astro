---
// <details>/<summary> natif : ouvrable au clavier et lu par les lecteurs
// d'ecran sans JavaScript.
import { Plus } from "lucide-react";

interface Props {
  title: string;
  class?: string;
}

const { title, class: className = "" } = Astro.props;
---

<details class={`accordion ${className}`}>
  <summary class="accordion-header">
    {title}
    <Plus className="accordion-icon" strokeWidth={1} aria-hidden="true" />
  </summary>
  <div class="accordion-content">
    <slot />
  </div>
</details>
